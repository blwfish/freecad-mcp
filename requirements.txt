# MCP Protocol (bridge dependency)
mcp==2.2.0
mcp-events>=0.1.0
# Not yet published to PyPI -- pinned to a commit, not a version. Same pin
# as kicad-mcp and jmri-mcp use.
mcp-agent-notes @ git+https://github.com/blwfish/mcp-agent-notes.git@dd83adf8bb97755f91dbfcf857dbaeb987883f8f

# FreeCAD's Python bindings come with FreeCAD (PySide2/PySide6)
# Don't install separately - use FreeCAD's version
